const files = require('../utils/files');

// Paths
const installPath = './shared/pipes';
const srcPath = './blueprints/pipe';
const srcFiles = {
  ts: '__name__.pipe.ts',
  spec: '__name__.pipe.spec.ts',
};

// Command
export default function(name, options) {
  // todo: copy files with specified name
  // todo: place files in ./shared/pipes
}