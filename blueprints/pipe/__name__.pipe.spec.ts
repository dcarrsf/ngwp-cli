import { <% classname %> } from './<% name %>.pipe';

describe('Pipe: <% classname %>', () => {
  it('create an instance', () => {
    const pipe = new <% classname %>();
    expect(pipe).toBeTruthy();
  });
});
