import { <% classname %> } from './<% name %>.pipe';

describe('<% classname %> Pipe', () => {
  it('create an instance', () => {
    const pipe = new <% classname %>();
    expect(pipe).toBeTruthy();
  });
});
